/// A powerful and flexible Dart package for parsing and converting dynamic values to various types.
///
/// It provides both safe (`tryParse`) and explicit (`parse`) methods for robust type handling,
/// along with extensions on `dynamic` for convenient inline parsing.
library type_parser;

export 'src/extension/dynamic_extension.dart';
export 'src/type_parser_base.dart';
